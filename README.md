# aa141793

Bootstrap repository for Codex mobile tasks.

This repo exists so Codex can check out the default `main` branch from iPhone-created tasks. Keep private Mac setup details in the local Mac Setup workspace unless intentionally publishing them.
